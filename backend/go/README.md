# cerberus-example-app
Example backend &amp; frontend app using Cerberus authorization

# usage

docker compose build
docker compose run cerberus-example-app --cerberusApiKey=[your api key] --cerberusApiSecret=[your api secret]
